---
 - hosts: slave1
   sudo : yes
   name : play1
   tasks:
    - name: Install nginx
      apt : name=nginx state=latest
    - name: Copy website to slave2
      copy: src=/home/ubuntu/sports/  dest=/var/www/html  owner=root  group=root  mode=0644
