---
 - hosts: slave1
   sudo : yes
   name : play1
   tasks:
    - name: Install nginx
      apt : name=nginx state=latest

 - hosts: slave2
   sudo : yes
   name : play2
   tasks:
    - name : Install Apache2 webser
      apt  : name=apache2 state=latest
    - name : Add website file 
      script : hello.sh
    - name : Execute command 'Date'
      command: date
